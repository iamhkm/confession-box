# Disable host check for development (fixes webpack dev server issues)
DANGEROUSLY_DISABLE_HOST_CHECK=true

# Set the port (optional, defaults to 3000)
PORT=3000

# Skip preflight check
SKIP_PREFLIGHT_CHECK=true
